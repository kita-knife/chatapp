-- Iteration 1: minimal extensions for chat session storage.
-- pgvector / pg_trgm / pg_stat_statements will be added in later iterations
-- alongside RAG / vector search features.

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
